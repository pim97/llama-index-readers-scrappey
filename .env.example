# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored and must never be committed.

# Required for the opt-in live integration test (tests/test_readers_scrappey.py::test_live_scrape_example_com).
# Get a key at https://scrappey.com.
SCRAPPEY_API_KEY=
